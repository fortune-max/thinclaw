# Notes Plugin

Create, read, update, and delete personal notes. Works for todo lists, shopping lists, journal entries, ideas, or any text.

No external API — notes are stored in the bot's SQLite database, scoped per user.
